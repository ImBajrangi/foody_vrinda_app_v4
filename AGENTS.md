Run directly on connected Android device / emulator:
npm run android

Open the native project in Android Studio (Press the green ▶ Run button inside Android Studio):
npm run android:open

Build the production debug APK:
npm run android:build

Sync Web updates to Android:
npm run sync
